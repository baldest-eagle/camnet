# CamNet Sender — Python Dependencies
# Install: pip install -r requirements.txt

# Camera capture
opencv-python>=4.9.0

# mDNS / ZeroConf service announcement
zeroconf>=0.132.2

# System tray icon (headless operation)
pystray>=0.19.5
Pillow>=10.3.0

# WebRTC fallback transport
aiortc>=1.9.0
aiohttp>=3.9.5

# SRT streaming via ffmpeg-python wrapper
ffmpeg-python>=0.2.0

# Audio capture (for mic passthrough)
pyaudio>=0.2.14

# Async support
asyncio-mqtt>=0.16.2  # optional, for MQTT-based discovery alt

# Utilities
click>=8.1.7
loguru>=0.7.2
psutil>=5.9.8
